import './styles.css';
import React from 'react';

// export const Blok22Warmup = () => {
//   const scorer = (names, scores) => {

//     const upperNames = names.map((name) => name.toUpperCase());

//     const allLettersWithScore = scores.map((score) => score.letter);

//     return upperNames.filter((name) => {

//       return name.split('').filter((letter) => {

//         if (allLettersWithScore.includes(letter)) {

//           debugger;

//           return true;

//         } else {

//           return false;

//         }

//       });

//     });

//   };

//   console.log(scorer(['Janek','Zosia'], scorer));
export const Blok22Warmup = () => {
  return <div>ssss</div>;
};

//   export default Blok22Warmup;
